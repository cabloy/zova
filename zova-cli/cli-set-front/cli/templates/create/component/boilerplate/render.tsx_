import { BeanRenderBase, Local } from '@cabloy/front';
import type { Controller<%=argv.nameMeta.fullCapitalize%> } from './controller.js';
import { ScopeModule } from '<%=cli.helper.slashPrefixForPath(argv.nameMeta.parts.length+1)%>resource/this.js';

export interface Render<%=argv.nameMeta.fullCapitalize%> extends Controller<%=argv.nameMeta.fullCapitalize%> {}

@Local()
export class Render<%=argv.nameMeta.fullCapitalize%> extends BeanRenderBase<ScopeModule> {
  render() {
    return <div></div>;
  }
}
